window.html = (strings, ...values) => {
  const template = document.createElement('template');
  const contents = [];

  strings.forEach(string => {
    // Push the string into the contents.
    contents.push(string);


  });
};
